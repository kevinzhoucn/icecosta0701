<!-- <h1>Listing activities</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>Activity type</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @activities.each do |activity| %>
      <tr>
        <td><%= activity.title %></td>
        <td><%= activity.content %></td>
        <td><%= activity.activity_type %></td>
        <td><%= link_to 'Show', activity %></td>
        <td><%= link_to 'Edit', edit_activity_path(activity) %></td>
        <td><%= link_to 'Destroy', activity, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity', new_activity_path %> -->

<div class="ipacro-postcontent ipacro-postcontent-0 clearfix">
  <div class="ipacro-content-layout">
    <div class="ipacro-content-layout-row">
      <div class="ipacro-layout-cell layout-item-0" style="width:100%;height:140px;background-image:url(http://tigermed.net/images/2_01.jpg);background-size:100%;">
      </div>
    </div>
  </div>
    <style type="text/css">
      @import url('/css/tabbing.css');
    </style>
    <script type="text/javascript" src="/js/tabbing.js"></script>
  <div class="ipacro-content-layout">
    <div class="ipacro-content-layout-row">
      <div class="ipacro-layout-cell layout-item-0" style="width: 25%" >
        <ul style="margin:1em 0 1em 0;">
        <% @activities.each do |activity| %>
          <li id="label<%=activity.activity_type%>"><%=activity.activity_type%></li>
        <%end%>
        </ul>
      </div>
      <div class="ipacro-layout-cell layout-item-0" style="width:75%" id="tabs">
        <% @activities.each do |activity| %>
          <div id="content<%=activity.activity_type%>" class="layout-item-2" style="margin-top:1em;min-height:300px">
            <%=activity.activity_type%>
          </div>
        <%end%>
      </div>
    </div>
  </div>
</div>